"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.throwError = exports.parseArgs = void 0;
const color = __importStar(require("picocolors"));
const commander_1 = require("commander");
async function parseArgs(initData) {
    const { argsTemplate } = initData;
    const program = new commander_1.Command();
    program
        .name(`create-${initData.intro.name || ""}`)
        .description(`CLI to create a new ${initData.intro.name}`)
        .version("1.0.0")
        .argument("<folder>", "Name of the folder for the new Bolt project");
    program.exitOverride();
    program.configureOutput({
        // writeOut: (str) => {
        //   process.stdout.write(`${str}`);
        // },
        // don't log errors
        writeErr: (str) => {
            // process.stdout.write(`[ERR] ${str}`);
        },
        // Highlight errors in color.
        // outputError: (str, write) => {
        // write(errorColor(str));
        // },
    });
    for (const arg of argsTemplate) {
        let opt = new commander_1.Option(`-${arg.alias} --${arg.name}`, arg.describe);
        opt.required = arg.required;
        if (arg.type === "string") {
            program.addOption(opt);
        }
        else if (arg.type === "boolean") {
            opt.argParser((val) => (val || val === "true" ? true : false));
            program.addOption(opt);
        }
        else if (arg.type === "select") {
            opt.choices(arg.options.map((opt) => opt.value));
            program.addOption(opt);
        }
        else if (arg.type === "multiselect") {
            opt.choices(arg.options.map((opt) => opt.value));
            opt.argParser((val) => val.split(","));
            program.addOption(opt);
        }
    }
    try {
        program.parse();
        const folder = program.args[0]; // Retrieve the positional argument
        const options = program.opts();
        return { ...options, folder }; // Return options including the positional argument
    }
    catch (e) {
        return {};
    }
}
exports.parseArgs = parseArgs;
function throwError(arg, message, value) {
    const label = color.bgRed(` error `);
    const _arg = color.yellow(arg);
    const valueLabel = color.bgYellow(` value `);
    throw new Error(`\n${label} ${_arg} ${message}\n${valueLabel} ${_arg} was '${value}'\n`);
}
exports.throwError = throwError;
